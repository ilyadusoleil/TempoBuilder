import React from 'react';
import { css } from '@emotion/core';

const Logo = () => {
  return (
    <div
      css={css`
        margin-left: 20px;
        font-family: Bowlby;
      `}
    >
      TempoBuilder
    </div>
  );
};

export default Logo;
